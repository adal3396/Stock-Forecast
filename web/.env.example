# Public project URL (safe to expose to the browser)
NEXT_PUBLIC_SUPABASE_URL=https://wbnvcajfcmdzkjizljtb.supabase.co

# Server-only: Supabase → Project Settings → API → service_role (secret).
# Never commit real keys. Add the same vars in Vercel for production.
SUPABASE_SERVICE_ROLE_KEY=
