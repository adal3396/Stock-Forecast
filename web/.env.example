# Copy to .env.local for local dev. On Vercel, set the same variable in Project → Settings → Environment Variables.
MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster.mongodb.net/?retryWrites=true&w=majority
